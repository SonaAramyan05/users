export type User = {
    id?: number;
    firstName: string;
    lastName: string;
    age: string;
};
export type Message = {
    id: string;
    username: string;
    text: string;
    date: string;
};
